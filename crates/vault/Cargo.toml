[package]
name = "nostr-portable-vault"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nostr-portable-crypto = { path = "../crypto" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
